{
  "name": "socaio",
  "version": "1.0.0",
  "description": "AI-powered audience analysis tool",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node build.js",
    "build:local": "OPENAI_API_KEY=$(grep OPENAI_API_KEY .env | cut -d '=' -f2) node build.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}